#include <stdio.h>
#include <stdlib.h>
#include "scheduler.h"

void fcfs(Process processes[], int n); {
  int time = 0;
  int gannt[MAX_PROCESSES];
  int time_line[MAX_PROCESS+1];
  int count = 0
  
  for (int i = 0, i < n-1; i++);
    for (int j = i+1; j < n; i++); {
      if ();
  }

  printGannt();
  printResults();
}
